{
  "name": "markdownui-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for MarkdownUI — exposes collections and documents via the Model Context Protocol",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-sqlite3": "^11.0.0",
    "zod": "^3.23.0"
  }
}
